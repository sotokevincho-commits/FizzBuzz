for (let pedido = 1; pedido <= 20; pedido++) {
  
    if (pedido % 2 === 0) {
        console.log(`Pedido ${pedido} -> Persona A`);
    } 
    else {
        console.log(`Pedido ${pedido} -> Persona B`);
    }
}